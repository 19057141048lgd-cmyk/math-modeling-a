# 操作日志：按参考文献改进求解器（2026-09-24）

> 本文件记录 2026-09-24 这一轮改动的全部过程。
> - **第 1–9 节**：v1 → v2（按文献加入 `mc`、`blc`）。第 9 节是复审报告 6 个问题在 v2 上的复核结果和修改建议。
> - **第 10 节**：9/24 晚上按用户选择开始修复，形成 v3。**代码大多已改，但验证和重跑都没完成。**
> - **第 11 节**：9/25 只改文字：设计文档、README、本日志。
> - **第 12 节**：当前的待办清单。
> - **Git**：状态见第 7 节和第 10 节，有未推送、未提交的内容，需要处理。

## 1. 结论

- **改了什么**：候选池里新增两类策略，都来自 Kulagina 等人的 Memory-aware HEFT（CCGrid 2025）。
  - **内存感知聚簇（开关 `mc`）**：作业或簇的图输入超过片上容量一半时，把它拆开。
  - **BLC 优先级（开关 `blc`）**：优先级取向上秩加最大入边通信量。
- **效果**：全量 100 个用例，5 核平均加速比问题1 3.762 → **3.817**，问题2 4.322 → **4.421**，问题3 4.430 → **4.506**。收益集中在“一批作业共用一组权重”的用例上，最大的单项改进是 makespan 降低 55.8%（case_092，问题1、5 核）。
- **代价**：1200 个多核组合里变好 170 个、**变差 40 个**，其中变差超过 5% 的有 4 个，最大 +11.3%（case_097，问题2、3 核）。退化原因都是代理模型排错。每个组合的平均求解时间从 7.0 秒增加到 11.5 秒。
- **试过但没采用**：HEFT 原文第 6 节的关键子任务前瞻（`child`）净效果为零；调度时的 LRU 驻留跟踪（`mr`）会重复计费。两者代码都保留，只作消融选项，不进候选池。

## 2. 背景

- **文献**：用户选定 4 篇，原文和 Markdown 转换稿在本地的 `参考文献/`，仓库中只有文献清单 `参考文献/README.md`。

  | 论文 | 出处 | 本轮用途 |
  |---|---|---|
  | Topcuoglu 等，HEFT / CPOP | IEEE TPDS 2002（期刊，CCF-A） | 调度器的理论来源；第 6 节的关键子任务扩展已实测，无效 |
  | Kulagina、Benoit、Meyerhenke，Memory-aware HEFT | CCGrid 2025（会议） | **主要借鉴来源**：内存感知聚簇和 BLC |
  | Wilhelm、Pionteck，串并联分解映射 | HCW 2025（IPDPS 研讨会） | 模型驱动局部搜索，本轮未做 |
  | Zhu 等，多核 NPU 上的 LLM 推理服务 | arXiv 预印本，未见正式发表 | 只作引言中的硬件背景，无可移植方法 |

- **用户的选择**：实现“内存感知调度 + BLC”和“关键子任务前瞻”两项；用 23 个用例验证；文档暂不修改。

## 3. 为什么能提高：诊断过程

动手前先诊断 spill 最严重的 5 个用例（问题2、5 核：case_092、073、028、091、072）。所用脚本原在 `%TEMP%\mm_extract\diag_*.py`，现已整理到 `A题方案/实验记录/脚本/`。

1. **单个算子不是问题**：单算子的最大工作集只有 74 KB 到 295 KB，远小于 L1 的 512 KB。
2. **峰值驻留几乎全是图输入**：这些图输入从 DDR 读入，而且驻留时间很长。以 case_092 为例，峰值 954,976 B 中有 930,400 B 是长期驻留的图输入。
3. **多核切分没有减少 spill**：1 核单子图的 spill（case_092 为 66.0 MB）和 5 核总 spill（63.3 MB）差不多，只是被平摊到了各核。
4. **原因在图结构**：case_092 由 142 个独立作业组成，共用同一组 50 个权重（每个 73,728 B，合计约 3.6 MB）。case_073 和 case_028 是同样的结构。
   - 原切分规则中，计算量不超过 `split_ratio·W/N` 的作业整体成为一个簇，所有候选的 `split_ratio` 都 ≥ 0.1，而每个作业只占总量约 1%。
   - 官方在簇内按 DFS 序执行，也就是先把一个作业的 50 层跑完，再换下一个作业。
   - 结果是每换一个作业，50 个权重就要全部重读一遍。
5. **拆开以后的效果**：列表调度按向上秩弹出就绪簇，所有作业第 1 层的秩最高，于是执行顺序自然变成逐层推进，同一个权重的消费者连续执行，只需要短暂驻留。
   - 这正是 HEFTM“分配时就检查内存，而不是事后打分”的思想。
   - 我们把检查放在聚簇这一步：`graph.py` 中的 `build_clusters(..., mem_cap)`。

单用例实验：问题2、5 核，官方评估。

| 用例 | 原选择 makespan | 加 `mc` 后 | 变化 |
|---|---:|---:|---:|
| case_073 | 3,180,041 | 1,751,537 | −44.9% |
| case_092 | 1,765,171 | 786,057 | −55.5% |
| case_028 | 11,491,035 | 8,039,218 | −30.0% |
| case_091 | 1,898,921 | 1,836,911（`mc+mr`） | −3.3% |
| case_072 | 4,828,484 | 4,832,469 | 基本不变 |

- **case_091 和 case_072 为什么收益小**：它们是分块共享结构，每个作业用两块共享输入，每块被 3 到 32 个作业共用。
  - case_072 每个作业需要 2 × 188 KB = 376 KB，而 L1 只有 512 KB，相邻作业最多只能共用其中一块。
  - 所以每个作业至少要重读 188 KB，476 个作业约 89 MB，和当前的 spill（88.8 MB）基本相等，已经**接近下界**。
- **BLC 的作用**：它在强耦合小图上有效。例如 case_035 问题1、5 核的官方 makespan 从 87,296 降到 82,227（−5.8%）。

## 4. 代码改动清单

以下是 `A题方案/solver/` 下的改动。

| 文件 | 改动 |
|---|---|
| `graph.py` | `Graph` 新增 `tensor_pos`（张量所在的 L1 或 UB，DDR 按 UB 计）。`build_clusters` 新增参数 `mem_cap`：作业的图输入（去重后）超过上限时不整体保留；in-tree 合并时簇的图输入也不超过上限 |
| `sched.py` | `ListScheduler` 新增 `opts` 和 `mem_cap` 参数。`priority()` 实现 BLC；`_critical_child` / `_child_finish` 以及 `pinned` 实现关键子任务前瞻；`_retain` 实现每核按 L1/UB 维护的 LRU 驻留集合（`mr`）。不打开开关时，行为与原版完全相同 |
| `solve.py` | 新增 `MEM_FRAC = 0.5`、`flags_of`、`mem_cap`；`build_candidate` 新增参数 `opts`（用 `+` 连接多个开关）；`candidate_params` 新增参数 `hw`；`CANDIDATES` 中问题1 加 4 个候选，问题2 加 4 个，问题3 加 4 个（参数元组第 6 项为开关） |
| `run_batch.py` | 新增 `replace()`：兜底改名遇到 Windows 临时文件锁时，每 0.5 秒重试一次，最多 20 次。验证中 case_069 因为这个问题失败过两次 |

新增的候选如下，都挂在各问题最常被选中的参数上：

- 问题1：`('A',0.25,16,2.0,16,'mc')`、`('A',0.1,16,2.0,4,'mc')`、`('A',0.25,16,2.0,None,'blc')`、`('A',0.25,16,2.0,16,'blc')`
- 问题2：`('B',0.1,16,8.0,None,'mc')`、`('B',0.1,64,2.0,None,'mc')`、`('B',0.1,16,8.0,None,'blc')`、`('B',0.1,64,2.0,None,'blc')`
- 问题3：`('C',0.1,64,4.0,None,'mc')`、`('C',0.25,16,8.0,None,'mc')`、`('C',0.1,64,4.0,None,'blc')`、`('B',0.1,16,8.0,None,'mc')`

## 5. 两次方案对比

v1 是改动前（Git 标签 `v1.0`，提交 3f16209），v2 是本轮。两次运行方式相同：`--verify 0`、8 进程、同一台机器。

### 5.1 方案差别

| | v1 | v2 |
|---|---|---|
| 候选池 | 问题1、2、3 分别 12 / 9 / 10 个 | 分别 16 / 13 / 14 个（新增 `mc`、`blc`） |
| 小作业处理 | 计算量小就整体成簇 | 计算量小**且**图输入不超过片上容量一半，才整体成簇 |
| 优先级 | 向上秩 | 向上秩；另有 BLC 候选 |
| 选优、兜底和单调性保证 | 模型选优，再与少核方案、问题2 方案做官方兜底比较 | 不变 |

### 5.2 全量 100 个用例：平均加速比

| 问题 | 2 核 | 3 核 | 4 核 | 5 核 |
|---|---|---|---|---|
| 问题1 | 1.837 → 1.862 | 2.573 → 2.617 | 3.221 → 3.270 | 3.762 → **3.817** |
| 问题2 | 1.993 → 2.031 | 2.866 → 2.928 | 3.691 → 3.754 | 4.322 → **4.421** |
| 问题3 | 2.008 → 2.037 | 2.903 → 2.958 | 3.764 → 3.812 | 4.430 → **4.506** |

问题3 中“无 L2 makespan ÷ 只读 Cache makespan”按逐例比值取平均：

| | 1 核 | 2 核 | 3 核 | 4 核 | 5 核 |
|---|---|---|---|---|---|
| v1 | 1.0074 | 1.0082 | 1.0141 | 1.0230 | 1.0299 |
| v2 | 1.0010 | 1.0029 | 1.0109 | 1.0167 | 1.0217 |

v2 的这个比值变小，是因为 `mc` 同时减少了问题2 和问题3 的 spill 回读，而 Cache 原本正好在这类回读上占优。

其他指标：

- **逐行变化**：1200 个多核组合中变好 170 个、变差 40 个、持平 990 个（以 ±0.5% 为界）。变差超过 2% 的有 19 个，超过 5% 的有 4 个。
- **按新选中的开关归类**：

  | 被选中的候选 | 次数 | 变好 | 变差 | 平均变化 | 最差 |
  |---|---:|---:|---:|---:|---:|
  | `mc` | 145 | 94 | 16 | −9.83% | +11.3% |
  | `blc` | 81 | 43 | 16 | −1.12% | +7.8% |
  | 原有候选或兜底 | 974 | 33 | 8 | −0.15% | +4.1% |

  “原有候选或兜底”一类也有变化，是因为“沿用少核方案”和“沿用问题2 方案”这两种兜底会连带引入新方案。
- **5 核加速比低于 3.0 的用例数**：问题1 从 28 变为 27，问题2 从 9 变为 8，问题3 仍为 7。
- **5 核有 spill 的用例数**：问题1 从 15 变为 13，问题2 从 27 变为 24，问题3 从 27 变为 24。
- **5 核加速比超过 5 的用例数**：问题1 从 7 变为 9，问题2 从 12 变为 17，问题3 从 16 变为 21。这属于超线性，原因是官方单核基准本身 spill 严重，而多核的片上容量合计更大，并且 `mc` 进一步消除了 spill。写进论文时需要说明。
- **1 核重切分（附加实验，不是正式加速比）**：“单核基准 ÷ 1 核 makespan”的逐例平均，问题1 从 1.005 变为 1.016，问题2 从 1.016 变为 1.039，问题3 从 1.024 变为 1.040。
- **单调性**：1500 个组合的 makespan 随核数单调不增，违反的情况为 0。
- **求解时间**：每个组合平均 7.0 → 11.5 秒，最长 67 → 129 秒。

改进最大的组合：

| 组合 | 变化 | 原参数 → 新参数 |
|---|---:|---|
| case_092 问题1、5 核 | −55.8% | `('A',0.1,16,2.0,8)` → `('A',0.25,16,2.0,16,'mc')` |
| case_092 问题2、5 核 | −54.8% | `('B',0.1,16,8.0)` → `('B',0.1,64,2.0,None,'mc')` |
| case_083 问题2、3 核 | −44.5% | `('B',1.0,64,0.0)` → `('B',0.1,64,2.0,None,'mc')` |
| case_073 问题2、5 核 | −44.0% | `('B',0.1,16,1.0)` → `('B',0.1,64,2.0,None,'mc')` |
| case_092 问题3、5 核 | −42.1% | `('C',0.1,64,2.0)` → `('C',0.1,64,4.0,None,'mc')` |

退化最大的组合：

| 组合 | 变化 | 原参数 → 新参数 |
|---|---:|---|
| case_097 问题2、3 核 | +11.3% | `('B',0.1,64,2.0)` → `('B',0.1,16,8.0,None,'mc')` |
| case_097 问题3、3 核 | +8.9% | `p2_plan` → `('C',0.25,16,8.0,None,'mc')` |
| case_005 问题2、3 核 | +7.8% | `('B',1.0,16,4.0)` → `('B',0.1,64,2.0,None,'blc')` |
| case_096 问题2、5 核 | +5.7% | `('B',0.1,64,8.0)` → `('B',0.1,64,2.0,None,'blc')` |
| case_097 问题1、3 核 | +4.6% | `('A',0.1,16,2.0,4)` → `('A',0.1,16,2.0,4,'mc')` |
| case_100 问题2、4 核 | +4.4% | `('B',0.1,64,2.0)` → `('B',0.1,64,2.0,None,'blc')` |

### 5.3 23 个用例的验证集

验证集由 16 个代表用例、5 个 spill 严重用例（092、073、028、091、072）和 2 个强耦合弱项用例（035、069）组成。

| 问题 | 2 核 | 3 核 | 4 核 | 5 核 |
|---|---|---|---|---|
| 问题1 | 1.744 → 1.794 | 2.355 → 2.451 | 2.744 → 2.911 | 3.158 → **3.367** |
| 问题2 | 1.982 → 2.070 | 2.719 → 2.882 | 3.312 → 3.546 | 3.692 → **4.023** |
| 问题3 | 2.003 → 2.077 | 2.784 → 2.939 | 3.476 → 3.617 | 3.897 → **4.129** |

- **整体**：276 个多核组合中变好 63 个、变差 4 个，最大退化 +4.3%（case_050，问题2、3 核，由 `blc` 引起）。
- **16 个代表用例**：5 核平均加速比问题1 从 3.150 变为 3.170，问题2 从 3.828 变为 3.829，问题3 从 3.932 变为 3.952；变好 16 个，变差 4 个。说明新候选对普通用例几乎没有影响。
- **新增的 7 个用例**：5 核平均加速比问题1 从 3.176 变为 3.820，问题2 从 3.383 变为 4.467，问题3 从 3.816 变为 4.531；变好 47 个，变差 0 个。

### 5.4 消融（在单用例上做，官方评估）

| 开关 | 结论 |
|---|---|
| `mc` | 收益的主体，见第 3 节的表 |
| `mr` | 只在 case_091 上有约 3% 的额外收益，在 case_092 和 case_073 上没有额外作用。它和 `predict` 里的 spill 时间重复计费，模型永远不会选它 |
| `blc` | 在强耦合小图上有效，在普通用例上有增有减 |
| `child` | 问题1 的 4 个弱项用例中有 3 个变差；问题2 上好坏参半。23 个用例上被选中 5 次，其中 2 次退化，净效果 +0.1%，因此移出候选池。这与原论文“只在高通信比时略有好处”的结论一致 |

## 6. 过程记录（按时间顺序）

1. 读完 4 篇文献，写出“借鉴哪部分、改哪段代码、怎么验证”的方案，用户选定实现 `child` 和“内存感知 + BLC”。
2. 诊断 spill 来源（见第 3 节），然后做快速实验：把 `split_ratio` 设为 0 并使用细粒度，确认收益存在。
3. 实现 `mc`、`mr`、`blc`、`child`，在 spill 用例和弱项用例上逐个开关做官方对比。
4. 在 23 个用例上跑第 1 轮验证（含 `child`）。之后去掉 `child`，重算选中过它的 3 个用例。
5. 复查代码时发现作业级内存判定没有在作业内部去重，会高估图输入的大小。受影响的有 28 个用例（91 个作业），于是修正并重算验证集中受影响的 8 个用例。当时已经启动的全量运行被中止并删除。
6. case_069 两次因为 Windows 临时文件锁而失败，于是给 `run_batch.py` 加入改名重试。
7. 全量 100 个用例重跑完成，没有失败。
8. 应用户要求开始整理 Git，做到一半时用户叫停（见第 7 节）。

## 7. 结果文件位置与当前 Git 状态

**结果文件**（都在 `A题方案/` 下）：

| 路径 | 内容 |
|---|---|
| `results_all/` | **v2 全量正式结果**，1500 行。原来的 v1 全量结果已被替换 |
| `results_v1_对照/` | v1 的 `summary.csv`、三张表和两张图，从 Git 标签 `v1.0` 中只读取出，供对照用 |
| `results_val23/` | 23 个用例的验证结果；`batch_log.txt` 按顺序拼接了 5 轮运行日志并加了说明 |

- **校验**：两份 `summary.csv` 中每行 `eval_file` 指向的评估文件都存在，其中的 makespan 与汇总表逐行一致（1500 行和 345 行，缺失 0，不一致 0）。
- **路径修正**：已把结果文件里记录的旧目录名 `results_all_v2/`、`results_v2/` 改成当前目录名。

**Git 状态**（用户叫停时的状态，之后没有再做任何 Git 操作）：

- **本地提交**：新增提交 `9bc7a76`“按文献加入内存感知聚簇与 BLC 优先级候选。”，只包含 `solver/` 下的 4 个代码文件。**未推送**，本地比 `origin/main` 领先 1 个提交。
- **本地标签**：`v1.0` 指向 `3f16209`，也就是改动前的版本。**未推送**。
- **暂存区**：旧 `results_all/` 的 4630 个文件被标记为删除（`git rm`）。磁盘上的 `results_all/` 已经是 v2 结果，尚未加入暂存区。
- **未跟踪**：`results_all/`（v2）、`results_val23/`、`results_v1_对照/`、`参考文献/`，以及本日志。
- **GitHub 上**：仍然是 v1（`3f16209`），没有变化。
- **注意**：仓库是公开的。`参考文献/` 里有 IEEE 版权论文的 PDF，以及几篇论文的全文转换稿，不宜推送到公开仓库，建议只提交一份带链接的文献清单。
- **回退方法**：v1 的全部结果都在 Git 历史里。想恢复旧结果，可以执行 `git restore --source=v1.0 --staged --worktree -- "A题方案/results_all"`（会覆盖磁盘上的 v2 结果，执行前先备份）。想撤销本地的代码提交，可以执行 `git reset --soft HEAD~1`。

## 8. 本轮没做的内容

- **文献中未实现的方法**：
  - 串并联分解加模型驱动的局部搜索（Wilhelm 与 Pionteck）；
  - CPOP 关键路径钉核；
  - HEFT 插入式调度；
  - HEFTM 的 MM 遍历（原文显示它 makespan 更差，而且很慢）。
- **模型改进**：
  - `mr` 与 spill 预测重复计费的问题没有解决，所以没有启用；
  - `MEM_FRAC = 0.5` 没有调参；
  - 没有修 Cache 代理模型（见第 9 节问题 2）。
- **实验**：
  - 没有做完整的消融（去掉聚簇、局部性惩罚、细切段、Task 规模族）；
  - 没有在全量上跑朴素基线；
  - 没有做全量官方复核（`--verify`）。
- **文档**：`A题方案/A题方案设计.md` 和 `README.md` 中的数字仍是 v1 的（例如 3.76 / 4.32 / 4.43），和现在的 `results_all/` 不一致，需要更新。本轮的方法（`mc`、`blc`）也还没写进设计文档。

## 9. 已知缺陷与复审问题的现状

复审报告是 `A题修改版复审_2026-09-24.md`，审查对象为 v1 的压缩包。下面逐条给出它在 v2 上的复核结果，数据都在 v2 的 `results_all/` 上重新计算过。

| 编号 | 问题 | v2 状态 | 依据 |
|---|---|---|---|
| 1 | 问题3 的 1 核 Cache 相对比值缺失 | **仍存在** | `table_p3.md` 中 1 核一格仍为 `-`；附录仍只有 2–5 核；Cache 对比图仍使用归一化加速比。v2 的数据为 1 核 1.0010、2 核 1.0029、3 核 1.0109、4 核 1.0167、5 核 1.0217 |
| 2 | Cache 代理模型的时序和带宽问题 | **仍存在** | `sched.py` 中的 `CacheModel` 和命中路径本轮没有改。复审的最小复现照样成立：50 时刻查询 A，插入 100 时刻的 C 之前返回 True，之后返回 False |
| 3 | 续跑缓存不校验版本 | **仍存在**，而且本轮实际遇到了 | 缓存键仍然只有核数、问题和 `verify`。本轮两次靠手动删除 `rows/*.json` 来强制重算，如果忘了删就会汇总出旧成绩。v2 的 `rows` 里单核 speedup 与 CSV 一致（不一致 0 行），这一项在新结果里已经不存在 |
| 4 | “保证 Cache 不劣于无 L2”的说法 | **仍存在** | `run_batch.py` 第 90 行附近，以及设计文档第 192 行和第 287 行的表述都没改。复审提到的两个反例在 v2 中已经消失，但出现了新反例：case_058 问题3、3 核，无 L2 为 1,585,249，Cache 为 1,587,812 |
| 5 | 代理模型预测误差 | **仍存在，数值略有改善** | 剔除兜底后：问题1 偏差 −4.61%、平均绝对误差 5.07%、低估超过 10% 的有 86 个（v1 为 −4.92%、5.40%、101）；问题2 为 −2.53%、3.38%、37 个（v1 为 −2.26%、3.74%、35）；问题3 为 −2.09%、2.67%、20 个（v1 为 −1.36%、4.13%、27）。case_035 问题2 的 4、5 核仍低估 39.5% 和 43.0%。设计文档第 22 行“spill 精确，所以不调用官方也能可靠选优”的说法没有改。v2 的 40 个退化也都来自模型排错 |
| 6 | 消融与基线不足；上界 3.10 缺少推导 | **部分改善** | 新增了 `mc`、`blc`、`child`、`mr` 的消融（第 5.4 节）和 23 个用例的新旧对比。原来列出的消融和全量朴素基线仍未做；设计文档第 20、63、392 行的“上界 3.10”没有补推导，而且 v2 的数字（问题2、4 核为 3.754）也还没更新进去 |

本轮新增的缺陷：

- **A**：模型排错导致 40 个组合退化，其中 4 个超过 5%，最大 11.3%。
- **B**：`mr` 重复计费，`child` 无效。两者作为选项保留，没有删除。
- **C**：`MEM_FRAC` 是经验值，没有调参。
- **D**：求解时间增加约 60%。
- **E**：文档、README 与结果不一致，Git 状态未收尾（见第 7 节）。

修改建议：每条都**待用户确认后再做**，工作量是粗估。

1. **问题3 的 1 核比值**（小，不需要重跑调度）：
   - `run_batch.write_tables` 中问题3 的比值行补上 1 核；附录扩展到 1–5 核共 500 行；
   - `summary.csv` 为问题3 的行增加 `cache_relative_speedup` 列；
   - `plot` 中的 Cache 对比图改画 T_无L2 / T_Cache；
   - 用现有缓存重新生成表格和图即可。
2. **Cache 代理模型**（中等，要重跑问题3）：
   - `CacheModel` 改为按插入时刻维护 FIFO 队列（有序列表加前缀和），`present(t)` 只统计 t 之前的插入；
   - 命中读取改走一个共享的 Cache 带宽时间桶（复用 `DDRBins`，带宽 250）；
   - 先在 23 个用例上比较问题3 的排序和官方成绩，确认后再跑全量问题3。
3. **续跑缓存**（小）：
   - 缓存键加入代码、配置、用例文件和官方评估器的哈希；
   - 读取缓存时检查方案和评估文件是否还在；
   - 增加 `--force` 选项。
4. **保证的措辞**（小，只改文字）：改为“不劣于参与比较的 Cache 场景候选，包括问题2 的方案在 Cache 下的评估”，并如实列出 case_058 这个反例。
5. **预测误差与 case_035**（中到大）：
   - 对比 case_035 的逐算子官方时间线和模型时间线，定位低估的原因；
   - 改写设计文档第 22 行的因果说法；
   - 可以考虑“小图局部复核”：只对官方评估耗时很短的图，把模型排名前 2 的方案交官方比较。这样也能消除 v2 的大部分退化，额外时间不多。
6. **消融、基线与上界**（中）：
   - 在 23 个用例上补做原来列出的消融；
   - 在全量上补跑朴素基线；
   - 给“上界 3.10”补出推导公式和复算脚本，或者降格为近似分析，并用 v2 的数字更新。

## 10. 第二轮：按用户选择修复（2026-09-24 晚，v3，未验证）

用户选了第 9 节的全部 6 项修改，外加“更新设计文档和 README”。执行顺序与状态如下。

| 顺序 | 事项 | 状态 |
|---|---|---|
| 1 | 续跑缓存加版本校验和 `--force`（问题3） | **完成并测试**。4 项检查通过；已有 100 个单核基准只补写校验信息，没有重算 |
| 2 | Cache 代理模型按时刻处理插入、命中共享 Cache 带宽（问题2） | **代码完成**，7 项单元检查通过。新旧模型对比实验跑完 42 / 46 组，没有汇总 |
| 3 | 查 case_035，加小图局部复核（问题5） | **代码完成**。原因是 MTE3 按序发射，已加入模型；case_035 单例验证有效。`--verify-small` 已加，未在验证集上评估 |
| 4 | 23 个验证用例上的新旧对比 | **未完成**。第一次因内存耗尽丢了 16 个用例；第二次被用户停止 |
| 5 | 全量 100 个用例重跑 | 未开始 |
| 6 | 问题3 的 1 核比值（问题1） | **代码完成**（`run_batch.py`），要等下次全量运行后才进结果文件 |
| 7 | 全量朴素基线、上界 3.10、消融（问题6） | 基线脚本已改好，未执行；`dev_jobbound.py` 第一次运行因内存不足失败；消融未做 |
| 8 | 措辞和文档（问题4 与文档项） | 9/25 完成，见第 11 节 |

**代码改动**（均在 `A题方案/solver/`）：
- `run_batch.py`：`code_fingerprint`、`cached_rows`、`--force`、`--verify-small`；`cache_relative` 与表格、附录、Cache 对比图改为 1–5 核；`ROW_FORMAT = 3`。
- `evaluate.py`：单核基准缓存按官方评估程序和用例文件的指纹校验。
- `sched.py`：`CacheModel` 重写；MTE3 按序发射模型，同时改变场景 B/C 的选核代价。
- `graph.py`：新增 `n_ops`（含 COPY 的算子数），供 `--verify-small` 使用。
- `run_baseline.py`：支持 `--cases all`、`--out`、问题3 和续跑。
- 新增 `dev_jobbound.py`。

**MTE3 的诊断**（case_035 问题2、5 核，参数 `('B',1.0,64,0.0)`）：
- **现象**：官方时间线在约 2.8 万到 5.2 万周期之间，5 个核中有 4 个空等。
- **原因**：核2 上子图 390 约 2.7 万周期就已算完，但它的 COPY_OUT 在 MTE3 上排在大子图 367 之后，等到约 5.17 万周期才发出。
- **修正效果**：加入模型后，5 核官方 makespan 91,346 → 53,840，预测误差 −43% → −2.9%；4 核 96,489 → 61,627，−40% → −2.3%。
- 诊断脚本在 `A题方案/实验记录/脚本/diag_035*.py`。

**内存问题与 Git**：
- **起因**：实验两次失败，都是因为内存耗尽。编辑器每隔约 30 秒执行一次 `git diff --cached --numstat`，而暂存区里有旧 `results_all/` 的 4630 条删除记录，这些进程卡住后堆积到 104 个，约占 19 GB。
- **处理**：经用户同意，执行了一次 `git reset`，只清空暂存区。本地提交 `9bc7a76`、标签 `v1.0` 和工作区文件都没变，也没有推送。
- **之后**：编辑器又开始统计工作区里 `results_all/` 的改动，每个进程约 2.4 GB。于是在 `A题方案/` 下新建了 `.gitattributes`（`results*/** -diff`），让结果目录不做逐行比较。它是否生效还没验证；9/25 上午检查时没有残留的 git 进程。

**实验文件**：
- `results_val23_v3/`：第一次验证运行，14 / 23 个用例，已失效，只在本地。
- `A题方案/实验记录/输出/cache_exp/`：Cache 对比实验的 42 组结果，脚本为 `实验记录/脚本/exp_cache.py`，日志为 `实验记录/输出/cache_exp_log*.txt`。9/25 从临时目录整理过来。

## 11. 文字修改（2026-09-25）

用户选择这一步只做不需要重跑实验的修改。v2 的统计全部从 `results_all/summary.csv` 重新计算，脚本为 `A题方案/实验记录/脚本/` 下的 `a_full.py`、`a_full2.py`、`v2_extra.py`，只读取结果和图结构，不调用评估器。

- **`A题方案/A题方案设计.md`**：
  - 开头加版本说明（v1 / v2 / v3 分别对应哪些结果）。
  - 第 0 节和 7.4 节的全量数字全部换成 v2。
  - 3.1 节加内存感知聚簇 `mc`；3.3 节加 BLC，并说明 `child`、`mr` 为什么不进候选池；3.5 节更新候选数量。
  - 新增 7.6 节，列出 v3 未验证的改动；7.5、8、9、10 节相应更新。
- **复审问题4（措辞）**：3.5 节和 6.2 节改为“问题3 不劣于把问题2 方案放到问题3 评估器下的官方结果”，明确这不等于“不劣于无 L2”，并写出反例 case_058。`run_batch.py` 的注释本来就是这个口径，没有改。
- **复审问题5（选优说法）**：第 0 节原来写“spill 精确，所以不调用官方评估器也能可靠选优”，改为“spill 精确消除了 spill 带来的系统性低估，但调度误差仍在”，并列出问题1 偏乐观、case_035 和 40 个退化组合。7.3 节结论和第 9 节论文结构建议同步改写。
- **复审问题6（上界）**：1.4 节、第 0 节、7.4 节把“理论上界 3.10”降格为“按纯计算量做作业级 LPT 分配的估计值”，说明它不是严格上界。数值要等 `dev_jobbound.py` 跑通后再替换。
- **与 v1 不同、需要改写的结论**：
  - 只读 Cache 的收益整体变小：1.001–1.022 倍，v1 为 1.007–1.030 倍。
  - 单核基线有 spill 的用例，Cache 收益从 v1 的“更大”变为“更小”：5 核 1.016 倍对 1.025 倍。原因是 `mc` 减少了 spill 回读。
  - 问题3 慢于问题2 的组合从 2 个变为 1 个（case_058）。
  - 兜底从 89 次变为 94 次。
- **没有核对的**：v2 的 spill 字节是否仍与官方逐字节一致（`python -m solver.dev_spill results_all`）。文档中已注明“尚未重新核对”。
- **README.md**：顶部提示、全量数字、目录表、待办清单和续跑说明都已更新。

## 12. 待办清单（截至 2026-09-25）

按依赖顺序：

1. **重跑 23 个验证用例**（v3，`--force`，只用模型），与 `results_val23/` 对比。这一步决定 MTE3 模型和 Cache 修正能不能保留，是后面各步的前提。
2. **补齐 Cache 对比实验的 4 组并汇总**，确认新 Cache 模型在问题3 上选得更准。
3. **在验证集上评估 `--verify-small 2 6000`**，看它能挽回多少退化，再决定全量是否打开。
4. **全量 100 个用例重跑**。问题3 的 1 核比值列和新表、新图会随这一步生成。之后更新设计文档第 0 节、7.4 节、README 和本日志的数字。
5. **全量朴素基线**：`python -m solver.run_baseline --cases all`。
6. **消融**（23 个用例）：去掉 in-tree 聚簇、去掉局部性惩罚、问题2 粗切段、问题1 去掉 Task 规模族。
7. **重跑 `dev_jobbound.py`**，替换 3.10。
8. **核对 v2 或 v3 的 spill 字节**：`python -m solver.dev_spill results_all`。
9. **Git**：9/25 已按用户要求整理并推送（见第 13 节）。
10. **可选**：`mr` 与 spill 预测重复计费、`MEM_FRAC = 0.5` 调参、求解时间比 v1 长约 60%。

**运行注意**：一次只跑一个实验，最多 8 个进程；运行中留意 git 进程数量和可用内存。

## 13. 整理与推送（2026-09-25）

按用户要求整理有效资源并推送到 GitHub，分三次提交，便于回退：

1. **v2 结果**：替换 `results_all/`，新增 `results_v1_对照/`、`results_val23/`（不含 `eval/`）和 `A题方案/.gitattributes`。这个提交的代码和结果一致，打标签 `v2.0`。
2. **v3 代码**：`solver/` 的改动、`dev_jobbound.py`，以及 100 个单核基准的校验文件 `results/single/*.key`。**未验证。**
3. **文档与实验记录**：设计文档、README、本日志、`参考文献/README.md`，以及从临时目录整理过来的 `A题方案/实验记录/`。

**没有推送的**（已写进 `.gitignore`）：
- `参考文献/` 里的论文原文和全文转换稿：有版权。
- `results_val23/eval/`：约 1 GB，可以重跑生成。
- `results_val23_v3/`：已失效。

**回退**：
- `git checkout v1.0`：回到文献改进前的版本。
- `git checkout v2.0`：回到加入 `mc`、`blc` 后、代码和结果一致的版本。
